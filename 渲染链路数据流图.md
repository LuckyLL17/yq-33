# 手写字模拟器 — 完整渲染链路数据流图

## 一、全局数据流总览

```mermaid
flowchart TB
    subgraph INPUT["① 输入层"]
        A1["📄 文件上传<br/>docx / pdf / txt"]
        A2["⌨️ 文本编辑器<br/>TextEditor"]
        A3["📋 模板选择<br/>TemplatePicker"]
    end

    subgraph PARSE["② 文本解析层"]
        B1["TXT: FileReader.readAsText()"]
        B2["DOCX: mammoth.extractRawText()"]
        B3["PDF: pdfjs-dist.getTextContent()"]
        B4["sanitizeText()<br/>去BOM + 规范换行"]
    end

    subgraph STORE["③ 状态中心<br/>useWorkspaceStore (Zustand)"]
        S1["rawText 原始文本"]
        S2["字体配置<br/>selectedFontId / fontSize / inkColor / jitter*"]
        S3["纸张配置<br/>selectedPaperId / paperBgColor / lineColor / lineSpacing"]
        S4["排版配置<br/>margins / lineHeight / letterSpacing / paragraphSpacing"]
        S5["叠加配置<br/>signatures / stamps / annotations / decorations / filter"]
    end

    subgraph FONT["④ 字体加载层"]
        F1["Google Fonts<br/>@font-face 动态注入"]
        F2["自定义字体<br/>FontFace API 加载"]
        F3["系统字体<br/>document.fonts 检测"]
        F4["字体缓存<br/>loadedFonts Set"]
    end

    subgraph LAYOUT["⑤ 排版计算层"]
        direction TB
        C1{"markdownEnabled?"}
        C2["纯文本排版<br/>breakTextIntoLines()"]
        C3["Markdown排版<br/>parseMarkdown() → layoutMarkdown()"]
        C4["分页计算<br/>paginate() / paginateMarkdownLines()"]
        C5["pages: Line[][] / RenderLine[][]"]
    end

    subgraph RENDER["⑥ Canvas 渲染层"]
        direction TB
        D1["1️⃣ drawPaper()<br/>纸张背景 + 线格 + 装订线 + 纹理"]
        D2["2️⃣ drawHandwrittenPage() / drawMarkdownPage()<br/>逐字绘制 + 抖动模拟"]
        D3["3️⃣ applyFilter()<br/>像素级滤镜处理"]
        D4["4️⃣ drawSignaturesForPage()<br/>签名叠加"]
        D5["5️⃣ drawStampsForPage()<br/>印章叠加"]
        D6["6️⃣ drawAnnotationsForPage()<br/>批注叠加"]
        D7["7️⃣ drawDecorationsForPage()<br/>装饰叠加"]
    end

    subgraph OUTPUT["⑦ 输出层"]
        E1["🖥️ 实时预览<br/>HandwritingPreview"]
        E2["🖼️ 导出PNG<br/>Canvas.toBlob()"]
        E3["📑 导出PDF<br/>jsPDF + addImage()"]
    end

    A1 --> B1 & B2 & B3
    A2 --> B4
    A3 --> B4
    B1 & B2 & B3 --> B4 --> S1
    S1 --> C1
    C1 -- "false" --> C2 --> C4
    C1 -- "true" --> C3 --> C4
    C4 --> C5
    S2 --> F1 & F2 & F3
    F1 & F2 & F3 --> F4
    S2 & S3 & S4 --> C2 & C3
    F4 & C5 --> D2
    S3 --> D1
    S5 --> D3 & D4 & D5 & D6 & D7
    D1 --> D2 --> D3 --> D4 --> D5 --> D6 --> D7
    D7 --> E1 & E2 & E3
```

---

## 二、核心渲染管线详解

### 2.1 文本输入 → 纯文本提取

```mermaid
flowchart LR
    subgraph 输入源
        F["File 对象<br/>.docx / .pdf / .txt"]
        T["直接输入/粘贴"]
    end

    subgraph 解析引擎["useFileParser"]
        TXT["parseTxt()<br/>FileReader.readAsText()<br/>+ BOM处理"]
        DOCX["parseDocx()<br/>mammoth.extractRawText()<br/>{ arrayBuffer }"]
        PDF["parsePdf()<br/>pdfjs-dist<br/>getTextContent()"]
    end

    subgraph 清洗
        CLEAN["sanitizeText()<br/>removeBOM + normalizeNewlines"]
    end

    F --> TXT & DOCX & PDF
    T --> CLEAN
    TXT & DOCX & PDF --> CLEAN
    CLEAN --> STORE["rawText<br/>useWorkspaceStore.setText()"]
```

**关键源码**：
- [useFileParser.ts](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/hooks/useFileParser.ts#L1-L107) — 文件解析入口
- [textUtils.ts → sanitizeText()](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/textUtils.ts#L14-L16) — 文本清洗

---

### 2.2 字体加载管线

```mermaid
flowchart TB
    FONT_ID["selectedFontId"] --> GET_FONT["getFontById()<br/>从 builtInFonts / customFonts 查找"]
    GET_FONT --> BUILD_STACK["buildFontStack()<br/>构建 font-family 栈<br/>含 fallback 字体"]

    GET_FONT --> LOAD{"font.source?"}
    LOAD -- "google" --> GOOGLE["loadGoogleFont()<br/>动态创建 &lt;link&gt;<br/>fonts.googleapis.com/css2"]
    LOAD -- "custom" --> CUSTOM["loadCustomFont()<br/>new FontFace() + load()<br/>document.fonts.add()"]
    LOAD -- "system" --> SYS["checkFontAvailable()<br/>document.fonts.load()"]

    GOOGLE & CUSTOM & SYS --> WAIT["waitForFont()<br/>轮询字体可用性<br/>超时 3~5s"]
    WAIT --> CACHE["loadedFonts Set<br/>去重缓存"]

    CACHE --> RENDER["Canvas ctx.font 使用"]
```

**关键源码**：
- [fontLoader.ts](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/fontLoader.ts#L1-L143) — 字体加载核心逻辑
- [fontPresets.ts → buildFontStack()](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/fontPresets.ts) — 字体栈构建

---

### 2.3 排版计算双通道

系统根据 `markdownEnabled` 标志选择两条排版路径：

```mermaid
flowchart TB
    RAW["rawText"] --> SWITCH{"markdownEnabled?"}

    SWITCH -- "false<br/>纯文本模式" --> PURE["纯文本排版通道"]
    SWITCH -- "true<br/>Markdown模式" --> MD["Markdown排版通道"]

    subgraph PURE["纯文本排版通道"]
        direction TB
        P1["breakTextIntoLines()<br/>按段落分割 → 逐字测量 → 自动换行"]
        P2["每行数据<br/>{ text: string, isParagraphEnd: boolean }"]
        P3["paginate()<br/>按 pageHeight - margins 计算分页"]
        P4["输出<br/>pages: LineText[][]"]
        P1 --> P2 --> P3 --> P4
    end

    subgraph MD["Markdown排版通道"]
        direction TB
        M1["parseMarkdown()<br/>正则解析为 MarkdownBlock[]"]
        M2["layoutMarkdown()<br/>遍历 block → 合并样式 → 逐 segment 换行"]
        M3["输出 RenderLine[]<br/>{ segments, y, height, blockType,<br/>blockStyle, blockBackground, blockBorderLeft, ... }"]
        M4["paginateMarkdownLines()<br/>按高度分页 + Y坐标修正"]
        M5["输出<br/>pages: RenderLine[][]"]
        M1 --> M2 --> M3 --> M4 --> M5
    end

    P4 & M5 --> CANVAS["进入 Canvas 渲染"]
```

**纯文本排版数据流**：
1. [breakTextIntoLines()](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/hooks/useHandwritingRender.ts#L171-L214) — 使用离屏 Canvas `measureText()` 逐字测量宽度
2. [paginate()](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/hooks/useHandwritingRender.ts#L216-L238) — 按 `availableH` / `lineH` / `paragraphGap` 分页

**Markdown排版数据流**：
1. [parseMarkdown()](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/markdown/parser.ts#L94-L210) — 正则解析 `# 标题` / `> 引用` / `- 列表` / `**加粗**` 等语法 → `MarkdownBlock[]`
2. [layoutMarkdown()](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/markdown/markdownRenderer.ts#L147-L266) — 为每个 block 合并样式（[defaultMarkdownStyles](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/markdown/styles.ts#L3-L173)），按 segment 换行，产出 `RenderLine[]`
3. [paginateMarkdownLines()](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/markdown/markdownRenderer.ts#L268-L311) — 按 `pageHeight - margins` 分页，修正每页 Y 坐标偏移

---

### 2.4 Canvas 渲染七层叠加

每次渲染按严格顺序叠加 7 层内容：

```mermaid
flowchart TB
    subgraph LAYER["Canvas 渲染七层管线"]
        direction TB
        L1["🟫 Layer 1: 纸张背景<br/>drawPaper()"]
        L2["✍️ Layer 2: 手写文字<br/>drawHandwrittenPage() / drawMarkdownPage()"]
        L3["🎨 Layer 3: 滤镜效果<br/>applyFilter()"]
        L4["✒️ Layer 4: 签名叠加<br/>drawSignaturesForPage()"]
        L5["🔴 Layer 5: 印章叠加<br/>drawStampsForPage()"]
        L6["📝 Layer 6: 批注叠加<br/>drawAnnotationsForPage()"]
        L7["🎀 Layer 7: 装饰叠加<br/>drawDecorationsForPage()"]
    end

    L1 --> L2 --> L3 --> L4 --> L5 --> L6 --> L7

    L1 -. "实现" .-> D1_IMPL["纸张背景渲染细节"]
    L2 -. "实现" .-> D2_IMPL["手写文字渲染细节"]
    L3 -. "实现" .-> D3_IMPL["滤镜处理细节"]
```

#### Layer 1: 纸张背景渲染

```mermaid
flowchart TB
    P_TYPE{"paperType?"}
    P_TYPE -- "blank" --> BLANK["纯色填充<br/>fillRect(bgColor)"]
    P_TYPE -- "kraft" --> KRAFT["牛皮纸<br/>线性渐变 + fillColor"]
    P_TYPE -- "line" --> LINE["横线纸<br/>横线 stroke 循环"]
    P_TYPE -- "grid" --> GRID["方格纸<br/>横线 + 竖线 stroke 循环"]
    P_TYPE -- "dotted" --> DOT["点阵纸<br/>arc 填充圆点循环"]

    BLANK & KRAFT & LINE & GRID & DOT --> MARGIN{"showMargin?"}
    MARGIN -- "true" --> BINDING["装订线<br/>红色竖线 + 3个装订孔"]
    MARGIN --> NOISE["纸张纹理噪点<br/>半透明渐变叠加"]
```

#### Layer 2: 手写文字渲染（核心）

```mermaid
flowchart TB
    subgraph CHAR_LOOP["逐字渲染循环"]
        SEED["seededRandom(seed)<br/>确定性伪随机数"]
        SEED --> JITTER["Jitter 抖动参数计算"]
        JITTER --> J1["positionX/Y: 位置微偏移"]
        JITTER --> J2["size: 字号随机波动"]
        JITTER --> J3["rotation: 旋转微偏"]
        JITTER --> J4["baseline: 基线微偏"]
        JITTER --> J5["inkDensity: 墨迹浓度波动"]
        JITTER --> J6["inkColor: 墨色色彩波动"]
        JITTER --> J7["spacing: 字间距抖动"]
        JITTER --> J8["lineDrift: 整行漂移"]
        JITTER --> J9["lineTilt: 整行倾斜"]
        JITTER --> J10["halo: 光晕效果"]
        JITTER --> J11["dryBrush: 飞白效果"]
    end

    subgraph DRAW["单字绘制"]
        TRANS["ctx.translate(cx, cy)<br/>ctx.rotate(rot)"]
        FILL["ctx.fillText(ch, -cw/2, 0)<br/>rgba(inkR, inkG, inkB, alpha)"]

        HALO_CHECK{"halo > 0.1<br/>&& 偏移明显?"}
        HALO_DRAW["光晕层: 偏移+低透明度 fillText"]

        DRY_CHECK{"dryBrush > 0.1<br/>&& alpha < 0.88?"}
        DRY_DRAW["飞白层: 缩小字号+偏移 fillText"]

        TRANS --> FILL
        FILL --> HALO_CHECK
        HALO_CHECK -- "yes" --> HALO_DRAW
        HALO_CHECK --> DRY_CHECK
        DRY_CHECK -- "yes" --> DRY_DRAW
    end

    subgraph SQUEEZE["溢出压缩"]
        CALC_W["计算 totalW<br/>含 letterSpacing + spacingJitter"]
        COMPARE{"totalW > maxContentW?"}
        SQUEEZE_F["squeeze = maxContentW / totalW<br/>按比例压缩字符宽度"]
        NO_SQUEEZE["squeeze = 1"]
    end

    CHAR_LOOP --> DRAW
    DRAW --> SQUEEZE
```

**关键机制**：
- [seededRandom()](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/hooks/useHandwritingRender.ts#L20-L27) — 确定性伪随机数生成器，同页同位置始终产生相同抖动，保证渲染稳定性
- 12 个 Jitter 参数独立控制不同维度的手写模拟
- **光晕效果**：当字有旋转或位移时，叠加一层低透明度偏移文字模拟墨迹扩散
- **飞白效果**：当墨迹透明度较低时，叠加一层缩小字号的偏移文字模拟干笔效果
- **溢出压缩**：当行宽超出内容区时，按比例压缩避免越界

#### Layer 3: 滤镜效果

```mermaid
flowchart TB
    F_TYPE{"activeFilter?"}
    F_TYPE -- "none" --> SKIP["跳过"]
    F_TYPE -- "inkBleed" --> F1["墨迹晕染<br/>膨胀 + 墨色渗透"]
    F_TYPE -- "pencilSketch" --> F2["铅笔素描<br/>Sobel 边缘检测 + 噪点"]
    F_TYPE -- "penStroke" --> F3["钢笔笔触<br/>压力变化 + 邻域墨迹"]
    F_TYPE -- "brushStroke" --> F4["毛笔笔触<br/>圆形膨胀 + 干笔"]
    F_TYPE -- "watercolor" --> F5["水彩渲染<br/>随机半径扩散 + 湿润感"]
    F_TYPE -- "carbonCopy" --> F6["复写纸<br/>灰度 + 蓝色偏移"]
    F_TYPE -- "fountainPen" --> F7["钢笔墨痕<br/>墨流变化 + 羽化"]
    F_TYPE -- "crayon" --> F8["蜡笔涂鸦<br/>蜡质纹理 + 缺口"]
    F_TYPE -- "marker" --> F9["马克笔<br/>曼哈顿距离扩散 + 鲜艳化"]

    F1 & F2 & F3 & F4 & F5 & F6 & F7 & F8 & F9 --> PIXEL["getImageData() → 像素操作 → putImageData()"]
    PIXEL --> RESET["重置变换矩阵<br/>ctx.setTransform(DPR, 0, 0, DPR, 0, 0)"]
```

**关键源码**：[filterEffects.ts](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/filterEffects.ts#L1-L449) — 所有滤镜均为像素级操作，基于 `getImageData()` / `putImageData()`

---

### 2.5 叠加层渲染

```mermaid
flowchart TB
    subgraph SIGN["签名叠加"]
        S_LOAD["loadSignatureImages()<br/>dataUrl → HTMLImageElement"]
        S_FILTER["filter(pageIndex)"]
        S_DRAW["drawSignatureOnCanvas()<br/>ctx.drawImage() + 背景纸张"]
    end

    subgraph STAMP["印章叠加"]
        T_LOAD["loadStampImages()<br/>dataUrl → HTMLImageElement"]
        T_FILTER["filter(pageIndex)"]
        T_DRAW["drawStampOnCanvas()<br/>globalAlpha=0.92<br/>globalCompositeOperation='multiply'"]
    end

    subgraph ANNO["批注叠加"]
        A_FILTER["filter(pageIndex)"]
        A_DISPATCH{"annotation.type?"}
        A_PATH["path/highlight → drawPath()"]
        A_SHAPE["circle/rect → drawShape()"]
        A_LINE["line/arrow → drawLine()"]
        A_UNDER["underline/wavy → drawUnderline()"]
        A_TEXT["text → drawText()"]
    end

    subgraph DECO["装饰叠加"]
        D_LOAD["loadDecorationImages()<br/>SVG Blob → HTMLImageElement"]
        D_FILTER["filter(pageIndex)"]
        D_DRAW["drawDecorationsForPage()<br/>translate + rotate + drawImage"]
    end

    S_LOAD --> S_FILTER --> S_DRAW
    T_LOAD --> T_FILTER --> T_DRAW
    A_FILTER --> A_DISPATCH
    A_DISPATCH --> A_PATH & A_SHAPE & A_LINE & A_UNDER & A_TEXT
    D_LOAD --> D_FILTER --> D_DRAW
```

---

## 三、状态驱动渲染的数据流

```mermaid
flowchart LR
    subgraph UI_COMPONENTS["UI 组件层"]
        FS["FontSettings"]
        PS["PaperSettings"]
        LS["LayoutSettings"]
        FIS["FilterSettings"]
        DP["DecorationPanel"]
        SP["SignaturePanel"]
        STP["StampPanel"]
        AP["AnnotationPanel"]
        TE["TextEditor"]
        FU["FileUploader"]
    end

    subgraph STORE["Zustand Store<br/>useWorkspaceStore"]
        RAW["rawText"]
        FONT_CFG["字体配置<br/>selectedFontId<br/>fontSize<br/>inkColor<br/>jitterAmount / 12个jitter子参数"]
        PAPER_CFG["纸张配置<br/>selectedPaperId<br/>paperBgColor<br/>paperLineColor<br/>paperLineSpacing<br/>showBindingLine"]
        LAYOUT_CFG["排版配置<br/>margins<br/>lineHeight<br/>letterSpacing<br/>paragraphSpacing"]
        OVERLAY["叠加数据<br/>signatures[]<br/>stamps[]<br/>annotations[]<br/>decorationPlacements[]<br/>activeFilter + filterIntensity"]
        PAGE["页面状态<br/>currentPage<br/>totalPages<br/>zoomLevel"]
    end

    subgraph HOOK["useHandwritingRender"]
        RS["renderState (useMemo)"]
        COMPUTE["computePages() / computeMarkdownPages()"]
        RENDER["renderToCanvas()"]
        ALL["renderAllCanvases()"]
    end

    subgraph OUTPUT["输出"]
        PREVIEW["HandwritingPreview<br/>实时单页预览"]
        EXPORT["useExport<br/>多页全部渲染"]
    end

    FS --> FONT_CFG
    PS --> PAPER_CFG
    LS --> LAYOUT_CFG
    FIS --> OVERLAY
    DP & SP & STP & AP --> OVERLAY
    TE & FU --> RAW

    RAW & FONT_CFG & PAPER_CFG & LAYOUT_CFG & OVERLAY --> RS
    RS --> COMPUTE
    RAW & RS --> RENDER
    COMPUTE --> PAGE
    PAGE --> PREVIEW
    RENDER --> PREVIEW
    ALL --> EXPORT
```

**响应式更新机制**：
- Store 任意字段变更 → Zustand 触发订阅更新
- [useHandwritingRender](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/hooks/useHandwritingRender.ts#L426-L774) 通过 `useMemo` 派生 `renderState`
- `renderToCanvas` 被包裹在 `useCallback` 中，依赖 `renderState` + `rawText` + 所有 overlay 数据
- `useEffect` 监听 `renderToCanvas` 变化 + `currentPage` → 自动重绘 Canvas

---

## 四、渲染触发与执行流

```mermaid
sequenceDiagram
    participant User as 用户
    participant UI as UI组件
    participant Store as Zustand Store
    participant Hook as useHandwritingRender
    participant Font as fontLoader
    participant Canvas as Canvas 2D

    User->>UI: 修改字体/字号/纸张等
    UI->>Store: setFontSize() / setInkColor() / ...
    Store-->>Hook: Zustand 订阅触发重渲染

    Note over Hook: useMemo 重新计算 renderState
    Hook->>Font: loadFont(fontPreset)
    Font-->>Hook: 字体加载完成

    Hook->>Hook: computePages() 或 computeMarkdownPages()
    Hook-->>Store: setTotalPages(n)

    Note over Hook: useEffect 触发
    Hook->>Canvas: renderToCanvas(canvas, pageIdx)
    
    Canvas->>Canvas: 1. drawPaper() — 纸张背景
    Canvas->>Canvas: 2. drawHandwrittenPage() / drawMarkdownPage() — 手写文字
    Canvas->>Canvas: 3. applyFilter() — 滤镜
    Canvas->>Canvas: 4. drawSignaturesForPage() — 签名
    Canvas->>Canvas: 5. drawStampsForPage() — 印章
    Canvas->>Canvas: 6. drawAnnotationsForPage() — 批注
    Canvas->>Canvas: 7. drawDecorationsForPage() — 装饰

    Canvas-->>User: 实时预览更新
```

---

## 五、导出数据流

```mermaid
flowchart TB
    subgraph EXPORT_PNG["导出 PNG"]
        PNG_1["renderAllCanvases()<br/>遍历所有页"]
        PNG_2["每页创建离屏 Canvas<br/>完整7层渲染"]
        PNG_3["canvas.toBlob('image/png')"]
        PNG_4["a.download 触发下载<br/>手写字_文件名_第N页.png"]
    end

    subgraph EXPORT_PDF["导出 PDF"]
        PDF_1["renderAllCanvases()<br/>遍历所有页"]
        PDF_2["每页创建离屏 Canvas<br/>完整7层渲染"]
        PDF_3["canvas.toDataURL('image/jpeg', 0.92)"]
        PDF_4["new jsPDF({ unit: 'px', format: [w, h] })"]
        PDF_5["逐页 addPage() + addImage()"]
        PDF_6["pdf.save()"]
    end

    PNG_1 --> PNG_2 --> PNG_3 --> PNG_4
    PDF_1 --> PDF_2 --> PDF_3 --> PDF_4 --> PDF_5 --> PDF_6
```

**关键源码**：[useExport.ts](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/hooks/useExport.ts#L1-L122)

---

## 六、完整数据流图（压缩版）

```mermaid
flowchart LR
    INPUT["输入源<br/>文件上传 / 文本编辑 / 模板"] --> PARSE["文本解析<br/>mammoth / pdfjs / FileReader"]
    PARSE --> STORE["Zustand 状态中心<br/>rawText + 全部配置"]
    
    STORE --> FONT_LOAD["字体加载<br/>Google / Custom / System"]
    STORE --> LAYOUT_CALC["排版计算<br/>换行 + 分页"]
    
    FONT_LOAD --> RENDER["Canvas 渲染引擎<br/>useHandwritingRender"]
    LAYOUT_CALC --> RENDER
    
    RENDER --> L1["Layer1: 纸张背景"]
    L1 --> L2["Layer2: 手写文字<br/>逐字+抖动"]
    L2 --> L3["Layer3: 滤镜效果<br/>像素操作"]
    L3 --> L4["Layer4: 签名"]
    L4 --> L5["Layer5: 印章"]
    L5 --> L6["Layer6: 批注"]
    L6 --> L7["Layer7: 装饰"]
    
    L7 --> PREVIEW["实时预览<br/>HandwritingPreview"]
    L7 --> EXPORT_PNG["导出 PNG"]
    L7 --> EXPORT_PDF["导出 PDF"]
```

---

## 七、关键数据结构

| 数据结构 | 定义位置 | 用途 |
|----------|---------|------|
| `WorkspaceState` | [useWorkspaceStore.ts](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/store/useWorkspaceStore.ts#L150-L296) | 全局状态，包含所有渲染参数 |
| `RenderState` | [useHandwritingRender.ts#L53-L73](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/hooks/useHandwritingRender.ts#L53-L73) | 渲染时使用的聚合状态 |
| `JitterParams` | [useHandwritingRender.ts#L37-L51](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/hooks/useHandwritingRender.ts#L37-L51) | 12维抖动参数集合 |
| `CharDrawInfo` | [useHandwritingRender.ts#L250-L265](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/hooks/useHandwritingRender.ts#L250-L265) | 单字绘制的全部计算结果 |
| `MarkdownBlock` | [markdown/types.ts](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/markdown/types.ts#L52-L59) | Markdown 解析后的块结构 |
| `RenderLine` | [markdown/markdownRenderer.ts#L6-L27](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/markdown/markdownRenderer.ts#L6-L27) | Markdown 排版后的渲染行 |
| `Annotation` | [useWorkspaceStore.ts#L105](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/store/useWorkspaceStore.ts#L105) | 批注联合类型 |
| `DecorationPlacement` | [types/index.ts#L15-L25](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/types/index.ts#L15-L25) | 装饰放置位置信息 |

---

## 八、性能优化关键点

1. **确定性随机数** — [seededRandom()](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/hooks/useHandwritingRender.ts#L20-L27) 保证同页同位置抖动一致，避免重绘闪烁
2. **离屏Canvas测量** — [breakTextIntoLines()](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/hooks/useHandwritingRender.ts#L171-L214) 使用独立 Canvas 做文字测量，不干扰主渲染
3. **DPR=2 高清渲染** — Canvas 物理尺寸 = 逻辑尺寸 × 2，`setTransform` 统一缩放
4. **字体加载去重** — [loadedFonts Set](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/fontLoader.ts#L3) + [loadingPromises Map](file:///Volumes/ExMac/traeProject/全站1/yq-33/src/utils/fontLoader.ts#L4) 避免重复加载
5. **图片预加载缓存** — 签名/印章/装饰图片通过 Promise 预加载到内存，渲染时直接 drawImage
6. **useMemo/useCallback** — renderState 和核心函数均做缓存，减少无效重计算
7. **滤镜在文字层之后、叠加层之前** — 避免对签名/印章等叠加元素误施滤镜
